# 5. Solicitar números al usuario hasta que ingrese el cero. Por cada uno,
# mostrar la suma de sus dígitos (utilizando una función que realice dicha
# suma).

# numero = 1234
# desglose = [int(digito) for digito in str(numero)]

# print(sum(desglose))

"""def suma_digitos(numero):
    while True:
        if numero == 0:            
            break
        else:
            desglose = [int(digito) for digito in str(numero)]
            print(sum(desglose))
        numero = int(input("Introduce un número: "))
            
numero1 = int(input("Introduce un número para comenzar: "))
suma_digitos(numero1)"""

# PENDIENTE HACER CON NUMEROS QUE SE VAN ALMACENANDO Y SUMANDO ENTRE SI HASTA QUE SE INTRODUCE 0